# InspectOps — 검증 가능한 제조·의료 검사 운영 플랫폼 (제품 문서)

> 이 문서는 흩어진 프로젝트들을 **하나의 제품**으로 읽는 상위 서사다. Inspection Copilot이 그
> 제품의 **런타임 코어**(멀티에이전트 오케스트레이터)이고, 다른 프로젝트들은 모듈로 붙는다.
> 한 줄: **"동작하는 검사가 아니라, 검증되고 운영되는 검사."**

---

## 1. 제품 가설 — 중소 제조/의료기기의 검사 AI 도입 4대 벽

검사 AI는 데모는 쉽지만 *현장 도입*에서 막힌다. 네 가지 벽을 한 제품으로 넘는다.

| # | 벽 | 현장의 말 | InspectOps의 답 |
|---|---|---|---|
| ① | **콜드스타트(라벨 부족)** | "결함 데이터가 거의 없어요" | 정상만 학습하는 **무지도 이상탐지**로 라벨 0개에서 시작 |
| ② | **신규 결함** | "본 적 없는 불량이 나와요" | **OOD 게이트 + conformal**로 "모를 때 멈춤", 신규를 사람검토로 |
| ③ | **규제 추적성** | "왜 그렇게 판정했는지 증빙이 필요해요" | **audit 로그 + 승격 게이트 + 설명(XAI)** — ISO 13485식 추적성 |
| ④ | **비전문가 운영** | "현장 작업자가 써야 해요" | **자연어 멀티에이전트 코파일럿** — 묻고·분석하고·근거로 답함 |

**척추 = 검증/신뢰.** 정확도 한 숫자가 아니라 *불확실성·근거·추적성·운영안전*을 갖춰야 규제·고위험
현장(제조·의료)에 올릴 수 있다 — 이게 제품의 차별점이자 만든 사람의 해자다.

## 2. 누가·왜 사는가

- **사용자**: 검사 자동화를 원하지만 데이터·인력·규제 대응이 부족한 **중소 제조사 / 의료기기 제조사**의 품질팀.
- **가치**: 라벨 없이 시작(①) → 신규 결함을 놓치지 않고(②) → 판정을 증빙하며(③) → 비전문가도 자연어로 운영(④).
- **시장 신호**: 이 제품을 실제로 파는 회사들이 타겟과 일치 — 뉴로클(머신비전 SW 플랫폼)·가우스랩스
  (반도체 계측 AI)·노타(엣지 경량화)·마키나락스(이상탐지)·루닛/뷰노(의료 AI 검증).

## 3. 모듈 맵 — 포트폴리오 프로젝트 → 플랫폼 역할

| 플랫폼 능력 | 출처 프로젝트 | 핵심 실측 |
|---|---|---|
| **검사 엔진(인라인)** | VLM Defect Inspector | 엣지 CNN CPU **1.8ms·99.6%·6MB**, ONNX INT8 |
| **검사 엔진(트리아지·설명)** | VLM Defect Inspector | Qwen2.5-VL QLoRA 33.7%→82.6%, 자연어 설명 |
| **콜드스타트(무지도 AD)** | VLM Defect / Industrial Anomaly | pixel AUROC **0.98**(정상만 학습), 지도 교차점 ≈ 라벨 20개 |
| **신규결함 게이트(OOD)** | VLM Defect | confidence 0.68 → Mahalanobis **0.97**(적발 38%→84%) |
| **통계적 보장(conformal)** | VLM Defect | LAC 1−α 보장, **Mondrian로 최소 클래스 0.79→0.91** |
| **규제 추적·자가개선** | VLM Defect | audit·승격 게이트(99.3% 후보 안전회귀로 거부) · XAI |
| **라인 실시간 모니터링** | Industrial Anomaly AI | MQTT 스트리밍 + REST/WebSocket 대시보드 |
| **운영자 코파일럿(런타임 코어)** | **Inspection Copilot** | LangGraph supervisor 동적 라우팅 + 검증 게이트 |
| **데이터 분석** | nl2sql-analytics-agent | BigQuery 자연어 분석, dry-run 비용 가드, 데이터 마트 |
| **지식 그라운딩(SOP)** | TechDocRAG / Inspection Copilot | 멀티모달 RAG + 근거 거리 게이트 + 인젝션 가드 |

> Inspection Copilot은 *이미* 비전·분석·지식·리포트를 묶고(supervisor), trust 층은 VLM Defect의
> conformal/OOD를 포팅했다 — 즉 플랫폼의 ~80%가 이미 한 레포에 통합돼 있다.

## 4. 아키텍처 · 데이터 흐름

```
   현장 작업자 / 품질팀  ── 자연어 질문 + 결함 이미지
                 │
        ┌────────▼─────────┐
        │  Supervisor (코어) │  의도 파악 → 동적 라우팅 → 종합
        └─┬────┬─────┬────┬─┘
          ▼    ▼     ▼    ▼
       Vision Analytics Knowledge Report
       엣지CNN NL2SQL    SOP RAG   종합·신뢰도
       +OOD/  +dry-run  +근거게이트 +사람검토
       conformal        +인젝션가드  배지
          │    │     │    │
          └──── 검증 척추: audit · trust 게이트 · "모를 때 멈춤" · 자가개선 루프 ────┘
                      ▲                                   │
                 라인 스트리밍(MQTT) ── 이상 신호 ────────┘
```

**흐름:** 검사(vision) → 모니터(anomaly) → 분석(analytics) → 그라운딩(knowledge) → 리포트(report)
→ 자가개선(검토→재학습→게이트→승격). 모든 단계에 검증 척추가 가로지른다.

## 5. e2e 데모 (얇지만 진짜 도는, 비용 0)

`scenario.py`가 한 "검사 교대(shift)" 시나리오를 **단일 Supervisor로 끝까지** 흘린다:
결함 이미지 검사 → 라인별 통계 → SOP 근거 → 종합 리포트, 그리고 인젝션/오프토픽은 게이트가 멈춤.
키 없이 결정적으로 돈다(프로젝트의 eval 철학과 동일 — 통합 *동작*을 재현 가능하게 시연). 같은 실행의
트레이스를 모아 **운영 대시보드**(라우팅 분포·게이트 정지율·지연 p50/p95)를 `traces/dashboard.html`로 생성한다.

```bash
python scenario.py                              # 통합 흐름 1회 시연 + 운영 대시보드 생성(오프라인·결정적)
python -m app.trace traces/scenario.jsonl --html  # 트레이스 → 대시보드만 따로 렌더
```

라인 모니터링 핸드오프는 `python monitor_demo.py` — 센서 스트림 이상탐지(시뮬레이션) →
가장 이상 잦은 라인의 처리 SOP를 코파일럿이 그라운딩(모니터→코파일럿 통합). 실 MQTT는
`app.monitoring.MqttSource`(동일 `.stream()` 인터페이스)로 교체 — MQTT-ready.

자가개선 루프는 `python improvement_demo.py` — needs_human 건이 검토 큐로 → 교정 라벨이 임계에
닿으면 재학습 트리거 → **안전 우선 승격 게이트**(정확도 99.3% 후보도 위험 회귀면 거부, v7만 승격).
게이트 로직은 결정적으로 돌고, 후보 스탯은 VLM Defect 실측(v4/v5/v7)을 인용한다.

**통합 제품 UI는 `python inspectops.py`**(Gradio·HF Spaces 진입점) — 위 세 흐름(코파일럿·라인
모니터링·자가개선)과 운영 대시보드를 **네 개 탭 한 화면**으로 묶어, 흩어진 CLI 데모가 아니라
*하나의 제품으로* 클릭해 돌릴 수 있게 한다. 코파일럿 단일 화면은 `python demo.py`, 서빙은
`uvicorn app.server:app`.

## 6. 차별점 (해자)

**제조·의료 도메인 × AI 구현·검증·경량화.** 일반 챗봇/검사 데모와 달리 ① 라벨 없는 콜드스타트
② 신규결함 OOD ③ 규제 추적성(ISO 13485 의료기기 경력) ④ 엣지 경량화(임베디드 C 경력)를 *동시에*
다룬다. "결과를 산업 기준으로 검증·책임할 수 있는 검사 AI."

## 7. 정직한 범위 (숨기지 않음)

- 이건 team-of-one 포트폴리오 **통합 데모**다. 각 모듈은 독립 레포로도 살아있고(개별 카드), 여기선
  *하나의 제품으로 어떻게 합쳐지는가*(시스템 사고)와 **런타임 코어의 실제 e2e 동작**을 보인다.
- 일부 모듈(라인 MQTT 스트리밍·VLM 트리아지)은 코어 데모에 결정적 스텁/별도 레포로 연결한다 —
  과대주장하지 않는다. 전부 무료(샌드박스·HF Spaces·로컬).

## 8. 타겟 회사 매핑

| 트랙/회사 | InspectOps에서 강조할 것 |
|---|---|
| 머신비전 플랫폼(뉴로클) | 전체 플랫폼 서사 + 모듈맵(이 문서 자체) |
| 반도체 계측(가우스랩스) | OOD·conformal·자가개선·WM-811K 전이 |
| 엣지 경량화(노타) | 엣지 CNN 1.8ms·ONNX INT8·right-sizing |
| 이상탐지(마키나락스·포스코DX) | 무지도 AD·MQTT 스트리밍·검증 게이트 |
| 의료 AI(루닛·뷰노) | 추적성·conformal·XAI·ISO 13485 |

## 9. 로드맵 (다음 무료 단계)

- ✅ **통합 제품 UI(`inspectops.py`)** — 코파일럿·라인 모니터링·자가개선·운영 대시보드를 네 개 탭 한 화면으로 묶은 HF Spaces 진입점. 흩어진 CLI 데모 → *하나의 제품*. 전부 키 없이 결정적 동작(코파일럿 탭만 키 있으면 실 SQL).
- ✅ **관측성 대시보드 통합** — `scenario.py`/`inspectops.py` 운영 탭이 운영 지표(라우팅 분포·게이트 정지율·지연 p50/p95) 집계·표시.
- ✅ **라인 모니터링 → 코파일럿 핸드오프** — `app/monitoring.py`(롤링 z-score·지속성 탐지, 플러그블 소스: Simulated 기본 / MQTT-ready) + `monitor_demo.py`(이상 라인 → SOP 그라운딩). 새 라우팅 에이전트 없이 통합(eval 회귀 0).
- ✅ **자가개선 루프** — `app/improvement.py`(검토 큐 + 재학습 트리거 + 안전 우선 승격 게이트) + `improvement_demo.py`. 정확도 99.3% 후보도 위험 회귀면 거부, v7만 승격(VLM Defect 게이트 포팅).
- ✅ **MCP 서버** — 검사 도구(SOP 검색·인젝션 가드·SQL 검증)를 Model Context Protocol로 노출(`mcp_server.py`). 검증 척추가 도구에 함께 실림(인젝션·근거부족 시 needs_human). 어떤 MCP 클라이언트에서도 호출.
- ⬜ 실 MQTT 브로커 연결(MqttSource 구현 — 브로커 필요). ← 유일 잔여(브로커 의존, 무료 데모 범위 밖)
