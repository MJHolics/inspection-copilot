# === 코어(현재 배선된 코드가 실제로 쓰는 것만) ===
# 오케스트레이션은 표준 라이브러리로 충분(LangGraph는 선택, 아래 참조).
google-genai>=0.3        # Analytics LLM (Gemini 무료 티어 기본)
reportlab>=4.0           # Report PDF (내장 한글 CID 폰트)
onnxruntime>=1.17        # Vision 엣지 CNN 추론(torch 불요, CPU 수 ms)
numpy>=1.26              # Vision 전처리
pillow>=10.0             # Vision 이미지 로드
fastapi>=0.110           # 서빙
uvicorn>=0.30            # ASGI 서버
gradio>=5.9              # 데모 UI (HF Spaces)
pydantic>=2.0

# === 선택(해당 페이즈에서 활성화) ===
# -- LangGraph 그래프 실행(app/graph.py, 동적 라우팅 시각화) --
# langgraph>=0.2
# langchain-core>=0.3
# -- 벡터(의미) 검색 업그레이드(Knowledge: TF-IDF 베이스라인 → dense) --
#    app/retrieval_vector.py: 기본 ko-sroberta-multitask(한국어), BGE-M3 등으로 교체 가능.
# sentence-transformers>=2.7
# chromadb>=0.5            # (대안) 내장 ONNX 임베딩 검색
# -- Vision 감지 업그레이드(YOLO 박스 단위 검사) --
# ultralytics>=8.2
# -- 다른 LLM 제공자 --
# anthropic>=0.40
# openai>=1.40

# === 개발/테스트 ===
pytest>=8.0
ruff>=0.5
httpx>=0.27              # FastAPI TestClient
