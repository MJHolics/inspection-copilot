# PLAN — 관련성/인젝션 가드 (적대적 eval 잔존 갭 닫기, 비용 0)

> 이 프로젝트는 이미 eval·가드레일·트레이싱·CI·배포가 성숙하다. 남은 단 하나의 *측정된* 실패는
> 적대적 셋의 **프롬프트 인젝션**(`adv-inject-en`)이 dense 그라운딩 게이트를 통과하는 것(0.42 > 0.40).
> README/카드가 스스로 다음 단계로 지목한 **"retrieval 위 전용 인젝션/관련성 가드(LLM 판정)"**를 구현한다.

## ① 목표·이유
검색 거리(그라운딩 게이트)는 *관련 없음*은 잘 거르지만 *적대적이지만 유사함*(인젝션이 SOP 근처에
임베딩)은 못 가른다. 임계를 올리면 동의어(0.46)·다국어(0.52) 정상 질의가 깨진다 — 즉 **거리 하나로는
'무관'과 '적대적 유사'를 분리 못 한다**(README의 정직한 잔존 갭). 해법은 **거리와 직교하는** 의도 가드.

## ② 설계 (검색 점수와 무관하게 *의도*로 판정)
- **층1 — 결정적 룰 가드(순수·오프라인·키 불요):** 다국어 인젝션 패턴("ignore previous instructions",
  "reveal system prompt", "이전 지시 무시", "시스템 프롬프트 출력" 등) 탐지 → needs_human.
- **층2 — LLM 관련성 판정(선택·주입):** 검사 도메인 질의인지 LLM이 판정(무료 Gemini 등). 룰을 빠져나간
  교묘한 오프토픽까지 잡되, 실패 시 막지 않음(룰이 1차 방어).
- **핵심 불변식:** 가드는 정상 결함 질의(골든·동의어·다국어)를 **절대 막지 않는다**. 단위테스트로 강제.

## ③ 측정
적대적 셋을 가드 적용 전/후로 재측정(`--suite adversarial`). 기대:
- dense에서 `adv-inject-en` gate X→O → 적대적 gate/e2e 상승(README 표의 인젝션 1건 잔존 → 해소).
- 골든셋 전 지표 1.00 **유지**(회귀 가드), 동의어·다국어 케이스 **불변**(가드가 직교임을 입증).

## ④ Phase
| P | 내용 | 산출 | 상태 |
|---|---|---|---|
| P1 | `app/guard.py`(룰 인젝션 탐지 + LLM 관련성 판정) + `KnowledgeAgent` 배선 + 단위테스트 6개(인젝션 차단 ∧ 정상 질의 불통과 불변식) | 가드 모듈 + 테스트 | ✅ |
| P2 | 적대적 eval 재측정 + README/카드 서사 갱신 | 갱신 수치·문서 | ✅ |

## ⑤ 결과 (실측, 2026-06-29)
- **적대적(dense): gate 0.92→1.00, e2e 0.92→1.00, grounding 1.00 유지.** `adv-inject-en` 차단됨.
- **직교성 입증:** 동의어(crz·pit)·다국어(multiling-en) 전부 ground=O 유지 — 가드가 정상 질의를 안 깨뜨림.
- **회귀 0:** 골든셋 전 지표 1.00 유지 · 전체 테스트 93 passed(+6 가드).
- 재현: `python -m app.eval.run_eval --suite adversarial --retriever dense`

> 비용 0: 룰 가드는 순수, LLM 판정은 무료 Gemini 키. dense 측정은 sentence-transformers 있을 때.
